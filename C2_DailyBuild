node {
    try {
        dir("${env.WORKSPACE}/../${env.JOB_NAME}@script/"){

            stage('Deploy User') {
                sh "python Main.py Deploy User"
            }


        }
    } catch (err) {
        currentBuild.result = 'FAILED'
        throw err
    }
}
